import type { NumberInvoiceRange } from '../../types/NumberInvoice';

// RxDB schema removed — PostgREST architecture handles invoice numbers via DB sequence

export type NumberInvoiceRangeDocument = NumberInvoiceRange;