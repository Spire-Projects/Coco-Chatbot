# PostgREST Configuration File
# Documentation: https://postgrest.org/en/latest/configuration.html

# AWS RDS PostgreSQL Connection
# Note: password characters ? and # must be URL-encoded in URI
db-uri = "postgres://appleladnd:Programador12pam@appleland.cbusqiwsy85i.us-east-2.rds.amazonaws.com:5432/postgres?sslmode=require"

# Database schema
db-schema = "public, app"

# Database pool size
db-pool = 10

# Server port
server-port = 3000

# Server host
server-host = "127.0.0.1"

# JWT settings
jwt-secret = "Pass123AppleLand_SuperSecretKey32!"
# jwt-aud eliminado: el JWT generado por app.login() no incluye campo `aud`

# Database role to use for requests
db-anon-role = "web_anon"

# Enable CORS
server-cors-allowed-origins = "*"

# SSL for AWS RDS
db-extra-search-path = "public, app"
